version: '2.4'
services:
  ocr-server:
    build: 
      context: .
      target: dev
    ports: 
      - "5004:5000"
    volumes:
      - ./src:/src/src
      - ./img:/img
      - ./numbers:/numbers
    environment:
      - PYTHONUNBUFFERED=1